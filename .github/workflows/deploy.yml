name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: Placeholder
        run: echo "Deployment to PythonAnywhere and Vercel will be configured here"
